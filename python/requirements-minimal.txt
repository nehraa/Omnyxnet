# Minimal requirements for testing (without torch which is huge)
pycapnp>=1.0.0
numpy>=1.24.0
click>=8.1.0
pyyaml>=6.0

# GUI dependencies for desktop app
kivy>=2.2.0
kivymd>=1.1.1

## Minimal extras for live testing
# These are small additions to enable live audio/video demos
sounddevice>=0.4.8
opencv-python>=4.8.0

## Minimal addition for QUIC support
aioquic>=0.9.19